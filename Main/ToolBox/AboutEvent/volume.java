// Get data
// Save(buff) data
// Refresh(merge) data, when "반납" react.
public class volume{
    public final ZERO= -1;
    private int[100] vol;
    

    public void setVol(){
        for(int i=0; i<100; i++) vol[i]= 0;
    }

    // When voume is above zero, block a 대출 btn
    public void inforceZero(int index){
        int current_state= vol[index];
        if ()
    }
}